# План итеративной разработки MVP "BrashLens"
## 15 итераций с деплоем в конце каждой

**Принцип:** Каждая итерация = работающая функция + деплой + тестирование в продакшн-окружении

---

## 🔧 ИТЕРАЦИЯ 1: "Скелет инфраструктуры" (3-5 дней)

### Задачи
1. Создать Git репозиторий + базовую структуру проекта
2. Настроить Docker Compose (PostgreSQL, Redis, FastAPI, Celery worker)
3. FastAPI: базовое приложение с `/health` и `/docs`
4. PostgreSQL: подключение, тестовая таблица
5. Redis: подключение, тестовый ключ
6. Celery: базовый worker, тестовая задача
7. Регистрация Telegram бота, webhook
8. Бот отвечает на `/start` сообщением "Привет"

### Деплой
- Настройка VPS (Ubuntu, Docker)
- Nginx reverse proxy
- SSL сертификаты
- CI/CD: базовый скрипт деплоя (git pull + docker-compose restart)
- Автозапуск сервисов через systemd

### Проверка работоспособности
- ✅ Бот отвечает в Telegram
- ✅ `/docs` FastAPI доступен
- ✅ PostgreSQL принимает подключения
- ✅ Celery выполняет тестовую задачу
- ✅ Логи пишутся корректно

---

## 👤 ИТЕРАЦИЯ 2: "Регистрация пользователей" (3-4 дня)

### Задачи
1. Модели БД: `User` (telegram_id, username, first_name, role, language, created_at)
2. Alembic: инициализация, первая миграция
3. Бот: обработка `/start`
   - Проверка существования пользователя
   - Если новый → выбор роли (кнопки "Я фотограф" / "Я клиент")
   - Сохранение в БД
4. API endpoints: `GET /api/users/me` (получение текущего пользователя)
5. Определение языка из Telegram settings

### Деплой
- Миграция БД на продакшн сервере
- Обновление Docker контейнеров
- Проверка webhook бота

### Проверка работоспособности
- ✅ Новый пользователь может выбрать роль
- ✅ Данные сохраняются в БД
- ✅ Повторный `/start` не создает дубликаты
- ✅ API возвращает данные пользователя

---

## 📸 ИТЕРАЦИЯ 3: "Регистрация фотографа" (4-5 дней)

### Задачи
1. Модель БД: `Photographer` (user_id, display_name, city, currency, timezone, avatar_url)
2. Бот: диалоговый flow регистрации фотографа
   - Выбор языка (RU/EN)
   - Ввод имени (с предложением из Telegram)
   - Ввод города
   - Выбор валюты (RUB/USD/EUR/THB)
   - Создание профиля с дефолтными настройками
3. Генерация персональной ссылки: `t.me/brashlens_bot?start=username`
4. Вывод ссылки фотографу с инструкцией
5. API: `POST /api/photographers`, `GET /api/photographers/me`

### Деплой
- Новая миграция БД
- Обновление логики бота
- Тестирование полного flow регистрации

### Проверка работоспособности
- ✅ Фотограф проходит регистрацию от начала до конца
- ✅ Получает персональную ссылку
- ✅ Данные корректно сохраняются
- ✅ Валидация входных данных работает

---

## 🎨 ИТЕРАЦИЯ 4: "Mini App - основа" (4-5 дней)

### Задачи
1. React проект (Vite + TypeScript + Tailwind)
2. Интеграция Telegram WebApp SDK
3. Базовая структура: роутинг, layout, навигация
4. Страницы-заглушки:
   - Dashboard (главная)
   - Profile (настройки)
   - Portfolio (портфолио)
   - Calendar (календарь)
   - Clients (клиенты)
5. API клиент (axios + react-query)
6. Аутентификация через Telegram WebApp initData
7. Мультиязычность (i18next): RU/EN

### Деплой
- Сборка production build
- Размещение статики на сервере (Nginx)
- Кнопка в боте "Открыть приложение" → Mini App
- HTTPS настройка для WebApp

### Проверка работоспособности
- ✅ Mini App открывается из бота
- ✅ Отображается корректно на мобильном
- ✅ Навигация работает
- ✅ Смена языка работает
- ✅ API запросы проходят с аутентификацией

---

## 🖼️ ИТЕРАЦИЯ 5: "Профиль фотографа + аватар" (3-4 дня)

### Задачи
1. File Storage: настройка HDD сервера + Cloudflare R2 (fallback)
2. API: загрузка аватара
   - Валидация (формат, размер)
   - Оптимизация (resize до 800x800)
   - Сохранение: HDD → R2 при недоступности
   - Генерация публичного URL
3. Mini App: страница "Профиль"
   - Редактирование имени, описания, города
   - Загрузка аватара (drag-drop)
   - Контактные данные (телефон, email, Instagram)
   - Кнопка "Сохранить"
4. Celery задача: оптимизация изображений

### Деплой
- Настройка HDD монтирования
- Конфигурация Cloudflare R2
- Миграция для новых полей
- Обновление API и Mini App

### Проверка работоспособности
- ✅ Фотограф может загрузить аватар
- ✅ Изображение оптимизируется
- ✅ Fallback на R2 работает при недоступности HDD
- ✅ Данные профиля сохраняются и отображаются

---

## 📷 ИТЕРАЦИЯ 6: "Портфолио - загрузка фото" (4-5 дней)

### Задачи
1. Модели БД: `Portfolio`, `PortfolioItem` (photo_url, thumbnail_url, order, category)
2. API:
   - `POST /api/portfolio/photos` (множественная загрузка)
   - `GET /api/portfolio/photos` (список с пагинацией)
   - `DELETE /api/portfolio/photos/{id}`
   - `PATCH /api/portfolio/photos/{id}/order` (изменение порядка)
3. Celery задачи:
   - Генерация thumbnail (400x400)
   - Оптимизация оригинала
   - Сохранение метаданных
4. Mini App: страница "Портфолио"
   - Grid с фотографиями
   - Drag-drop загрузка
   - Прогресс-бар загрузки
   - Возможность удаления
   - Изменение порядка (drag-and-drop)
5. Бот: загрузка фото через чат
   - Отправка фото в личку боту
   - Автоматическое добавление в портфолио

### Деплой
- Миграция БД
- Увеличение лимитов Nginx для upload
- Настройка Celery очереди для обработки изображений

### Проверка работоспособности
- ✅ Загрузка через Mini App работает
- ✅ Загрузка через бота работает
- ✅ Thumbnails генерируются
- ✅ Изменение порядка сохраняется
- ✅ Удаление работает корректно

---

## 📅 ИТЕРАЦИЯ 7: "Календарь доступности" (4-5 дней)

### Задачи
1. Модель БД: `PhotographerSettings` (работа с календарем)
   - working_days (json: [1,2,3,4,5,6,7])
   - working_hours_start, working_hours_end
   - booking_buffer (минуты между съемками)
   - advance_booking_days (макс дней вперед для брони)
2. Модель БД: `BlockedDate` (заблокированные даты/периоды)
3. API:
   - `GET /api/calendar/availability` (получение доступных слотов)
   - `POST /api/calendar/block` (блокировка дат)
   - `DELETE /api/calendar/block/{id}`
   - `PATCH /api/photographers/settings/calendar`
4. Mini App: страница "Календарь"
   - Настройки рабочих дней/часов
   - Календарь с возможностью блокировки дат
   - Визуализация занятых/свободных слотов

### Деплой
- Миграции БД
- Обновление API
- Обновление Mini App

### Проверка работоспособности
- ✅ Настройки календаря сохраняются
- ✅ Блокировка дат работает
- ✅ API корректно рассчитывает свободные слоты
- ✅ Визуализация понятна и удобна

---

## 📦 ИТЕРАЦИЯ 8: "Типы съемок и пакеты" (3-4 дня)

### Задачи
1. Модели БД:
   - `ShootType` (название, описание, длительность)
   - `Package` (shoot_type_id, название, цена, описание, что_входит)
2. API:
   - CRUD для типов съемок
   - CRUD для пакетов
   - `GET /api/photographers/{username}/packages` (публичный endpoint)
3. Mini App: управление пакетами
   - Список типов съемок
   - Создание/редактирование пакетов
   - Указание цены, длительности, описания
4. Бот: команда `/prices` → показ списка пакетов

### Деплой
- Миграции БД
- Обновление API и Mini App
- Обновление логики бота

### Проверка работоспособности
- ✅ Фотограф создает типы съемок
- ✅ Фотограф создает пакеты с ценами
- ✅ Клиент видит цены через бота
- ✅ Публичный API отдает пакеты

---

## 🔗 ИТЕРАЦИЯ 9: "Регистрация клиента через ссылку" (3-4 дня)

### Задачи
1. Модель БД: `Client` (user_id, photographer_id, первый_контакт)
2. Бот: обработка deep links
   - Парсинг `?start=photographer_username`
   - Поиск фотографа по username
   - Автоматическое создание клиента
   - Автосвязывание клиента с фотографом
   - Приветственное сообщение с кнопками:
     - "📸 Посмотреть портфолио"
     - "📅 Записаться на съемку"
     - "💰 Узнать цены"
3. API: `GET /api/clients` (для фотографа - список его клиентов)

### Деплой
- Миграция БД
- Обновление логики бота
- Тестирование deep links

### Проверка работоспособности
- ✅ Клиент переходит по ссылке фотографа
- ✅ Автоматически создается связь
- ✅ Показываются корректные кнопки действий
- ✅ Фотограф видит клиента в своей базе

---

## 🖼️ ИТЕРАЦИЯ 10: "Публичное портфолио для клиентов" (4-5 дней)

### Задачи
1. API: `GET /api/photographers/{username}/portfolio` (публичный)
2. Mini App: публичная страница портфолио
   - Grid с фотографиями
   - Fullscreen просмотр (swipe между фото)
   - Информация о фотографе (имя, город, описание, контакты)
   - Кнопка "Записаться на съемку"
3. Бот: кнопка "📸 Посмотреть портфолио" → открывает Mini App
4. Оптимизация загрузки изображений:
   - Lazy loading
   - Progressive images
   - WebP формат

### Деплой
- Обновление API
- Обновление Mini App
- CDN настройка для изображений (опционально)

### Проверка работоспособности
- ✅ Клиент может открыть портфолио
- ✅ Фото загружаются быстро
- ✅ Fullscreen режим работает
- ✅ Информация о фотографе отображается

---

## 📝 ИТЕРАЦИЯ 11: "Бронирование съемок" (5-6 дней)

### Задачи
1. Модель БД: `Booking`
   - photographer_id, client_id, package_id
   - date, time_start, time_end
   - status (pending, confirmed, cancelled, completed)
   - client_name, client_phone, notes
   - created_at, updated_at
2. API:
   - `POST /api/bookings` (создание брони)
   - `GET /api/bookings` (список броней: для фотографа - все, для клиента - только свои)
   - `PATCH /api/bookings/{id}/status` (изменение статуса)
   - `GET /api/photographers/{username}/availability` (свободные слоты с учетом броней)
3. Mini App: форма бронирования для клиента
   - Выбор пакета
   - Календарь с доступными датами
   - Выбор времени из свободных слотов
   - Форма контактных данных
   - Поле для пожеланий
   - Подтверждение брони
4. Бот: уведомления
   - Фотографу: новая бронь
   - Клиенту: подтверждение брони

### Деплой
- Миграция БД
- Обновление API
- Обновление Mini App
- Настройка уведомлений

### Проверка работоспособности
- ✅ Клиент видит свободные слоты
- ✅ Бронирование создается
- ✅ Двойное бронирование невозможно
- ✅ Уведомления доставляются
- ✅ Календарь обновляется после брони

---

## 👥 ИТЕРАЦИЯ 12: "CRM для фотографа" (4-5 дней)

### Задачи
1. Mini App: страница "Клиенты"
   - Список всех клиентов
   - Поиск по имени
   - Фильтры (с бронью/без, активные/завершенные)
   - Карточка клиента:
     - Контактная информация
     - История броней
     - Заметки фотографа
     - Кнопка "Написать в Telegram"
2. Mini App: страница "Брони"
   - Список броней (сегодня, предстоящие, прошедшие)
   - Фильтры по статусу
   - Возможность изменения статуса
   - Календарный вид
3. API:
   - `PATCH /api/clients/{id}` (обновление заметок)
   - `GET /api/bookings/today`, `/upcoming`, `/past`

### Деплой
- Обновление API
- Обновление Mini App

### Проверка работоспособности
- ✅ Фотограф видит список клиентов
- ✅ Поиск и фильтры работают
- ✅ История броней отображается
- ✅ Заметки сохраняются
- ✅ Переход в чат Telegram работает

---

## 🔔 ИТЕРАЦИЯ 13: "Система уведомлений" (4-5 дней)

### Задачи
1. Модель БД: `Notification` (user_id, type, title, message, sent_at, read_at)
2. Celery задачи:
   - Напоминание клиенту за 24 часа до съемки
   - Напоминание фотографу за 1 час до съемки
   - Уведомление о смене статуса брони
3. Бот: отправка уведомлений через Telegram
4. API:
   - `GET /api/notifications` (список уведомлений)
   - `PATCH /api/notifications/{id}/read`
5. Mini App: колокольчик с непрочитанными
6. Настройки уведомлений:
   - Включение/выключение типов уведомлений
   - Время напоминаний

### Деплой
- Миграция БД
- Настройка Celery periodic tasks
- Обновление бота, API, Mini App

### Проверка работоспособности
- ✅ Напоминания отправляются вовремя
- ✅ Уведомления доставляются через бота
- ✅ Непрочитанные отображаются в Mini App
- ✅ Настройки работают

---

## 🖼️ ИТЕРАЦИЯ 14: "Галереи для передачи фото" (5-6 дней)

### Задачи
1. Модели БД:
   - `Gallery` (photographer_id, booking_id, name, access_token, expires_at)
   - `GalleryPhoto` (gallery_id, photo_url, thumbnail_url, order)
2. API:
   - `POST /api/galleries` (создание галереи)
   - `POST /api/galleries/{id}/photos` (загрузка фото)
   - `GET /api/galleries/{access_token}` (публичный доступ)
   - `DELETE /api/galleries/{id}/photos/{photo_id}`
3. Mini App: управление галереями (для фотографа)
   - Создание галереи для клиента
   - Загрузка фотографий (множественная)
   - Привязка к бронированию
   - Генерация ссылки доступа
   - Отправка ссылки клиенту
4. Mini App: просмотр галереи (для клиента)
   - Grid с фото
   - Fullscreen просмотр
   - Скачивание отдельных фото
   - Скачивание всего архива (zip)
5. Бот: отправка ссылки на галерею клиенту
6. Celery задача: создание zip архива

### Деплой
- Миграция БД
- Обновление API
- Обновление Mini App
- Тестирование загрузки больших объемов

### Проверка работоспособности
- ✅ Фотограф создает галерею
- ✅ Загружает фото (включая большие)
- ✅ Клиент получает ссылку
- ✅ Клиент может просматривать и скачивать
- ✅ Zip архив создается корректно

---

## 💳 ИТЕРАЦИЯ 15: "Система оплаты" (4-5 дней)

### Задачи
1. Модель БД: `Payment`
   - booking_id, amount, currency, status
   - payment_method (card_transfer, online, cash)
   - payment_details (реквизиты)
   - paid_at
2. API:
   - `POST /api/payments` (создание записи об оплате)
   - `PATCH /api/payments/{id}/status` (подтверждение оплаты)
3. Настройки фотографа:
   - Реквизиты для оплаты (номер карты, банк)
   - Способы оплаты (включить/выключить)
4. Mini App: оплата для клиента
   - Выбор способа оплаты
   - Если "перевод на карту" → показ реквизитов + кнопка "Я оплатил"
   - Если "наличные на месте" → просто отметка
   - Если "онлайн-оплата" → интеграция платежной системы (опционально для MVP)
5. Бот:
   - Уведомление фотографу об оплате
   - Клиенту: подтверждение получения оплаты

### Деплой
- Миграция БД
- Обновление API
- Обновление Mini App
- Тестирование флоу оплаты

### Проверка работоспособности
- ✅ Клиент видит способы оплаты
- ✅ Реквизиты отображаются корректно
- ✅ Статус оплаты обновляется
- ✅ Уведомления отправляются
- ✅ История платежей сохраняется

---

## 🎯 БОНУС: ИТЕРАЦИЯ 16: "Интеграция в канал + финальная полировка" (3-4 дня)

### Задачи
1. Бот: работа в группах/каналах
   - Добавление бота как админа
   - Закрепленное сообщение с командами
   - Обработка команд в группе: `/portfolio`, `/booking`, `/prices`
   - Эфемерные ответы (видны только автору)
   - Переадресация в личку для конфиденциальности
2. Обработка нестандартных запросов:
   - Клиент запрашивает занятую дату → уведомление фотографу
   - Свободный текст клиента → пересылка фотографу
3. Финальная полировка UX:
   - Проверка всех флоу
   - Исправление багов
   - Улучшение текстов
   - Оптимизация загрузки
4. Документация:
   - README для деплоя
   - API документация
   - Инструкции для пользователей

### Деплой
- Финальное обновление всех компонентов
- Полное тестирование системы
- Проверка под нагрузкой
- Подготовка к продакшн запуску

### Проверка работоспособности
- ✅ Бот корректно работает в группах
- ✅ Все уведомления доставляются
- ✅ Конфиденциальность соблюдается
- ✅ Вся система работает стабильно
- ✅ Документация актуальна

---

## 📊 ИТОГО

**Количество итераций:** 15 основных + 1 бонусная  
**Общее время разработки:** 60-75 рабочих дней (2.5-3.5 месяца)  
**Частота деплоя:** После каждой итерации (15+ деплоев)  
**Команда:** 1-2 разработчика

### Преимущества подхода:
✅ Всегда есть работающая версия в продакшн  
✅ Ранее обнаружение проблем  
✅ Возможность получать фидбек от пользователей  
✅ Низкий риск масштабных провалов  
✅ Постепенное наращивание функциональности  
✅ Легко корректировать план на основе реальности
